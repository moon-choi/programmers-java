package first;

public class Car {
	//필드는 객체의 속성.
	String name;
	int number;
	
	//생성자가 없는 상태에서는 컴파일러가 기본 생성자를 만듦.
	
	//생성자 만기 
	public Car(String x) {
		name = x; //이렇게 하면 Car라는 생성자가 만들어지면서 name이 초기화됌.
	}
}
