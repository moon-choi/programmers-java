package first;

public class Car2 {
	//필드는 객체의 속성.
	String name;
	int number;
	
	//생성자 만들기 
	public Car2(String x) {
		name = x; 
		//이렇게 하면 Car라는 생성자가 만들어지면서 name이 초기화됌.
		//Car.java에서 생성자를 추가했더니 기본생성자에서 오류남. => CarMaker2 참고 
		//더이상 기본생성자를 이용해서 Car객체 생성 불가. 
		//그래서 스트링 값을 매개변수로 넣어줘야함. 
	}
}
